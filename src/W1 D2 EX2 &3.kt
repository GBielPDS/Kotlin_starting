//Week 1, day 2, exercise 2 and 3
fun main() {
    print("Type a word: ")
    var word = readln()
    println("before $word")
    print("Type a new word: ")
    word = readln()
    println("After: $word")

    /* var is used when we need a value that will be changed.
    however val is used when the value will not be changed,
    so val is unmodified and var is modified.
     */
}